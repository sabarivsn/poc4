plugins {
    id 'java'
}

group 'poc4'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: '../build/libs', includes: ['*.jar'])
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
